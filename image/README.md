# Background images

Put your custom background files here:

- `desktop.png`, `desktop.jpg`, `desktop.jpeg`, `desktop.webp`, or `desktop.gif`
- `mobile.png`, `mobile.jpg`, `mobile.jpeg`, `mobile.webp`, or `mobile.gif`

The build script embeds them into the Worker. If a file is missing, that device type uses the default black background.
